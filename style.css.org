*{box-sizing:border-box}
:root{
 --night:#071b17;--night-deep:#03110e;--night-soft:#12362d;
 --green:#a6e93c;--green2:#c5ff68;--green-deep:#5d9f1d;
 --purple:#8063ad;--purple2:#a98ed1;
 --cream:#f3ead3;--paper:#fffaf0;--paper2:#f4ecd8;
 --ink:#183c31;--body:#33483f;--muted:#6e7b74;--line:rgba(24,60,49,.15);
 --shadow:0 18px 45px rgba(5,30,23,.16);--shadow-dark:0 20px 50px rgba(0,0,0,.38)
}
html{scroll-behavior:smooth}
body{margin:0;background:#f6efdd;color:var(--body);font-family:Georgia,"Times New Roman",serif;line-height:1.56}
img{display:block;max-width:100%}
a{color:inherit}
button,input,textarea{font:inherit}
.skip{position:fixed;top:8px;left:8px;z-index:1000;transform:translateY(-150%);background:#fff;padding:.6rem 1rem;border-radius:8px;color:#102d25}
.skip:focus{transform:none}
.shell{width:min(1220px,calc(100% - 40px));margin:auto}
.eyebrow{font:800 .68rem/1.2 Arial,sans-serif;text-transform:uppercase;letter-spacing:.19em;color:#668d1a;margin:0 0 .5rem}
.btn{display:inline-flex;align-items:center;justify-content:center;gap:.4rem;padding:.8rem 1.14rem;border-radius:999px;text-decoration:none;border:1px solid transparent;font:800 .73rem/1 Arial,sans-serif;text-transform:uppercase;letter-spacing:.045em;transition:.2s}
.btn:hover{transform:translateY(-2px)}
.btn-green{background:linear-gradient(#b7ef4a,#87ca27);color:#10230b;box-shadow:0 8px 22px rgba(86,130,25,.24)}
.btn-green:hover{background:linear-gradient(#c8ff67,#97da33)}
.btn-dark{background:#123a30;color:white;border-color:#2d5c50}
.btn-outline{background:rgba(255,255,255,.12);color:white;border-color:rgba(255,255,255,.52);backdrop-filter:blur(10px)}
.btn-paper{background:#fff;color:#173b31;border-color:rgba(21,58,47,.18);box-shadow:0 8px 24px rgba(5,36,27,.08)}
.btn-row{display:flex;gap:.72rem;flex-wrap:wrap;align-items:center}
.section-heading{max-width:760px;margin-bottom:34px}
.section-heading h1,.section-heading h2{font-size:clamp(2.4rem,5vw,4.8rem);line-height:.96;letter-spacing:-.04em;color:var(--ink);margin:.15rem 0 .8rem}
.section-heading p{font-size:1.06rem;color:var(--muted);margin:.2rem 0}

/* NAV */
.site-header{position:absolute;z-index:40;top:0;left:0;right:0;height:74px;display:flex;align-items:center;padding:0 clamp(22px,4vw,64px);background:linear-gradient(180deg,rgba(2,16,12,.68),rgba(2,16,12,0));color:#fff}
.brand{display:inline-flex;flex-direction:column;text-decoration:none;line-height:.76;text-shadow:0 2px 12px rgba(0,0,0,.45);min-width:150px}
.brand span{font:700 .72rem/1 Arial,sans-serif;letter-spacing:.42em;color:#eaf2e6}
.brand strong{font:700 1.58rem/.95 Georgia,"Times New Roman",serif;letter-spacing:.055em;color:var(--green)}
.main-nav{margin-left:auto;display:flex;align-items:center;gap:1.3rem}
.main-nav a{position:relative;text-decoration:none;font:800 .68rem/1 Arial,sans-serif;text-transform:uppercase;letter-spacing:.05em;padding:.85rem 0;color:#f3f6f3}
.main-nav a:after{content:"";position:absolute;left:50%;right:50%;bottom:.34rem;height:2px;background:var(--green);transition:.2s}
.main-nav a:hover:after,.main-nav a.active:after{left:0;right:0}
.main-nav a.active{color:#caff69}
.menu-btn{display:none;margin-left:auto;border:1px solid rgba(255,255,255,.55);border-radius:999px;background:#12392e;color:#fff;padding:.6rem .85rem;font:700 .72rem/1 Arial,sans-serif}

/* HOME HERO */
.home-hero{position:relative;height:clamp(650px,55vw,850px);min-height:650px;overflow:hidden;background:#0b251e;color:white}
.home-hero>img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;object-position:center 50%}
.home-hero:after{content:"";position:absolute;inset:0;background:
 linear-gradient(90deg,rgba(3,18,14,.92) 0%,rgba(3,18,14,.80) 28%,rgba(3,18,14,.43) 52%,rgba(3,18,14,.08) 78%),
 linear-gradient(180deg,rgba(3,18,14,.08),rgba(3,18,14,.04) 58%,rgba(3,18,14,.30))}
.hero-copy{position:relative;z-index:5;padding-top:180px;max-width:680px}
.hero-copy .eyebrow{color:#d0f57e}
.hero-copy h1{font-size:clamp(4.1rem,8vw,7.4rem);line-height:.82;margin:.15rem 0 .8rem;letter-spacing:-.055em;color:white;text-shadow:0 4px 25px rgba(0,0,0,.45)}
.hero-copy .tagline{font:800 .9rem/1 Arial,sans-serif;text-transform:uppercase;letter-spacing:.24em;color:#c6ff61;margin:0 0 1.2rem}
.hero-copy .lede{max-width:660px;color:#e3ece7;font-size:1.09rem;margin:0 0 1.7rem}
.hero-note{position:absolute;z-index:5;left:50%;bottom:32px;transform:translateX(-50%);color:#f4f8f3;font-style:italic;font-size:.98rem;padding:.65rem .95rem;border:1px solid rgba(255,255,255,.42);background:rgba(4,31,24,.55);border-radius:16px;backdrop-filter:blur(8px)}

/* TORN PAPER / INTRO */
.parchment{position:relative;z-index:10;margin-top:-16px;background:
 radial-gradient(circle at 10% 20%,rgba(93,102,62,.04) 0 1px,transparent 1.5px) 0 0/13px 13px,
 linear-gradient(90deg,#efe6cf,#fff9e9 48%,#eee3c7);color:#173b31}
.parchment:before,.parchment:after{content:"";position:absolute;left:0;right:0;height:24px;background:inherit;z-index:-1}
.parchment:before{top:-15px;clip-path:polygon(0 70%,4% 38%,8% 60%,13% 28%,18% 60%,22% 34%,27% 66%,33% 30%,39% 58%,44% 35%,50% 61%,56% 29%,62% 57%,68% 33%,73% 60%,79% 27%,85% 58%,91% 31%,96% 56%,100% 35%,100% 100%,0 100%)}
.parchment:after{bottom:-14px;clip-path:polygon(0 0,100% 0,100% 38%,96% 61%,91% 35%,86% 65%,81% 34%,76% 61%,70% 30%,64% 64%,58% 33%,52% 62%,46% 29%,40% 65%,34% 34%,28% 62%,21% 30%,15% 63%,8% 35%,3% 60%,0 38%)}
.home-intro{min-height:220px;display:grid;grid-template-columns:1.5fr repeat(3,.82fr) 1.18fr;width:min(1220px,calc(100% - 50px));margin:auto}
.home-intro>article{padding:31px 23px;border-right:1px solid rgba(78,91,55,.20);display:flex;flex-direction:column;justify-content:center}
.home-intro>article:last-child{border-right:0}
.home-intro h2{font-size:1.8rem;line-height:1.05;margin:.15rem 0 .55rem;color:#163e33}
.home-intro h3{font-size:1.08rem;margin:.28rem 0;color:#244f43}
.home-intro p{margin:.2rem 0 .7rem;font-size:.9rem;line-height:1.38}
.intro-icon{font:800 1.55rem/1 Arial,sans-serif;color:#79ab24}
.feature-card{position:relative;background:#0c3027!important;color:#fff!important;box-shadow:inset 0 0 50px rgba(0,0,0,.12)}
.feature-card .eyebrow{color:#bef06a}
.feature-card h2{color:white;text-align:center;font-size:1.52rem;letter-spacing:.02em}
.feature-card p{text-align:center;color:#dce9e2}
.feature-card .btn{align-self:center}

/* SHOWCASE */
.book-showcase{background:
 radial-gradient(circle at 50% 0,rgba(73,121,88,.4),transparent 48%),
 linear-gradient(#0c3128,#041814);color:white;padding:34px 34px 46px}
.book-showcase .ornament{text-align:center;font-size:1.35rem;color:#e8efe9;margin-bottom:20px}
.book-showcase .ornament span{color:var(--green)}
.cover-row{width:min(1150px,100%);margin:auto;display:grid;grid-template-columns:repeat(5,1fr);gap:24px}
.cover-link{text-align:center;text-decoration:none;color:white;min-width:0}
.cover-link img{width:100%;height:auto;border-radius:5px;border:1px solid rgba(255,255,255,.48);box-shadow:0 11px 24px rgba(0,0,0,.38);transition:.22s}
.cover-link:hover img{transform:translateY(-7px);box-shadow:0 16px 30px rgba(0,0,0,.48)}
.cover-link span{display:block;margin-top:9px;font:700 .75rem/1.25 Arial,sans-serif;letter-spacing:.02em;color:#eef5ef}
.showcase-cta{text-align:center;margin:26px 0 0}

/* SUBPAGE HERO */
.sub-hero{position:relative;min-height:365px;padding:138px 0 72px;color:white;background:
 linear-gradient(100deg,rgba(3,19,15,.91),rgba(5,48,38,.55)),
 url("/images/hero-sub.webp") center 48%/cover no-repeat;overflow:hidden}
.sub-hero:after{content:"";position:absolute;inset:auto 0 0;height:120px;background:linear-gradient(transparent,rgba(3,25,19,.58))}
.sub-hero .shell{position:relative;z-index:3}
.sub-hero h1{font-size:clamp(3.2rem,7vw,6.2rem);line-height:.9;margin:.15rem 0 .8rem;letter-spacing:-.045em}
.sub-hero p{max-width:730px;color:#dfe9e3;font-size:1.07rem}
.sub-hero .eyebrow{color:#c9f176}

/* SECTIONS */
.paper-section{background:
 radial-gradient(circle at 10% 20%,rgba(93,102,62,.038) 0 1px,transparent 1.5px) 0 0/14px 14px,
 linear-gradient(90deg,#f5edd9,#fffaf0 50%,#f1e7cf);padding:78px 0}
.night-section{background:
 radial-gradient(circle at 50% 0,rgba(103,146,82,.22),transparent 50%),
 linear-gradient(145deg,#0c342a,#041914);color:white;padding:78px 0}
.night-section .section-heading h2,.night-section h2,.night-section h3{color:white}
.night-section .section-heading p{color:#cad8d0}
.night-section .prose p{color:#d4dfd8}
.night-section .prose a{color:#c9f176}

.card-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:22px}
.card{border:1px solid rgba(21,59,48,.13);background:rgba(255,255,255,.83);border-radius:18px;box-shadow:0 12px 35px rgba(8,47,35,.08);overflow:hidden}
.card-body{padding:24px}
.card h3{font-size:1.5rem;margin:.15rem 0;color:#173f34}
.card p{color:#596b63}
.feature-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:24px}
.feature-box{padding:28px;border:1px solid rgba(255,255,255,.16);border-radius:18px;background:rgba(255,255,255,.055);box-shadow:inset 0 1px 0 rgba(255,255,255,.08)}
.feature-box .big-icon{color:#c2ef6b;font:800 1.7rem/1 Arial,sans-serif}
.feature-box p{color:#d4dfd8}
.split-feature{display:grid;grid-template-columns:1.08fr .92fr;gap:48px;align-items:center}
.split-feature h2{font-size:clamp(2.5rem,5vw,4.6rem);line-height:.95;letter-spacing:-.04em;color:var(--ink);margin:.2rem 0 1rem}
.split-feature p{font-size:1.06rem;color:#4c6157}
.split-art{position:relative}
.split-art img{width:min(360px,78%);margin:auto;border-radius:8px;box-shadow:0 24px 48px rgba(5,37,27,.25)}
.split-art:before{content:"";position:absolute;inset:10% 2% 5% 12%;border-radius:28px;background:linear-gradient(145deg,#d9e9b5,#9fcb5d);z-index:-1;transform:rotate(3deg)}

/* BOOKS GRID */
.books-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:24px}
.book-tile{text-decoration:none;transition:.2s}
.book-tile:hover{transform:translateY(-4px)}
.book-tile .cover-wrap{padding:20px 18px 0;background:linear-gradient(180deg,#e4dbc5,#f8f0df);display:flex;justify-content:center}
.book-tile img{width:100%;max-width:250px;margin:auto;border-radius:6px;border:1px solid rgba(63,73,51,.22);box-shadow:0 16px 30px rgba(0,0,0,.20)}
.book-tile .card-body{padding:22px 24px 27px}
.book-tile h3{font-size:1.48rem;line-height:1.1}
.book-num{font:800 .67rem/1 Arial,sans-serif;text-transform:uppercase;letter-spacing:.15em;color:#729522}
.status{display:inline-block;margin-top:.65rem;padding:.3rem .56rem;border-radius:999px;background:#e7f3cf;color:#476b0c;font:800 .64rem/1 Arial,sans-serif;text-transform:uppercase;letter-spacing:.07em}

/* ABOUT */
.about-layout{display:grid;grid-template-columns:1.15fr .85fr;gap:50px;align-items:start}
.prose h2{font-size:2.1rem;line-height:1.05;color:var(--ink);margin:0 0 1rem}
.prose h2:not(:first-child){margin-top:2.7rem}
.prose p{font-size:1.05rem;color:#40564c}
.prose a{color:#3f7315;font-weight:700}
.about-visual{position:sticky;top:22px}
.about-visual img{border-radius:22px;box-shadow:var(--shadow);border:1px solid rgba(33,63,51,.16)}
.fact-card{margin-top:22px;background:#0b3027;color:white;border-radius:18px;padding:25px;box-shadow:var(--shadow-dark)}
.fact-card h3{margin:.1rem 0 1rem;font-size:1.35rem}
.fact-list{display:grid;gap:.2rem}
.fact-row{display:flex;justify-content:space-between;gap:1rem;padding:.52rem 0;border-bottom:1px solid rgba(255,255,255,.12);font-size:.94rem}
.fact-row:last-child{border-bottom:0}
.fact-row span:first-child{color:#c6d5ce}
.fact-row span:last-child{text-align:right;font-weight:700}

/* BOOK DETAIL */
.book-hero{position:relative;min-height:400px;padding:142px 0 68px;color:white;background:
 linear-gradient(95deg,rgba(3,18,14,.94),rgba(4,42,33,.64)),
 url("/images/hero-sub.webp") center 48%/cover no-repeat}
.book-hero .shell{position:relative;z-index:2}
.book-hero h1{font-size:clamp(3rem,6vw,5.8rem);line-height:.9;letter-spacing:-.045em;margin:.15rem 0 .75rem;max-width:930px}
.book-hero .subline{color:#dbe9e0;font-size:1rem;margin:0}
.book-detail{display:grid;grid-template-columns:330px 1fr;gap:54px;align-items:start}
.cover-stack{position:sticky;top:24px}
.cover-large{width:100%;border-radius:8px;box-shadow:0 22px 45px rgba(4,42,31,.25);border:1px solid rgba(71,76,56,.28)}
.spec-card{margin-top:20px;background:white;border:1px solid var(--line);border-radius:16px;padding:20px 21px;box-shadow:0 12px 30px rgba(8,47,35,.07)}
.spec-card h3{margin:.1rem 0 .8rem;color:#173f34;font-size:1.18rem}
.spec-list{display:grid;gap:.1rem}
.spec-row{display:flex;justify-content:space-between;gap:1rem;padding:.5rem 0;border-bottom:1px dashed rgba(25,60,49,.16);font-size:.91rem}
.spec-row:last-child{border-bottom:0}
.spec-row span:first-child{color:#728078}
.spec-row span:last-child{font-weight:700;color:#27483c;text-align:right}
.book-copy{max-width:760px}
.book-copy .scare-tag{font:800 .68rem/1 Arial,sans-serif;text-transform:uppercase;letter-spacing:.16em;color:#6c901f;margin:0 0 1rem}
.book-copy p{font-size:1.06rem;color:#40564c}
.book-copy .pull{font-size:1.32rem;font-weight:700;color:#274d3f;font-style:italic;border-left:5px solid #94cd31;padding:12px 0 12px 22px;margin:1.8rem 0;background:linear-gradient(90deg,rgba(163,218,65,.10),transparent)}
.book-copy .availability-note{font-size:.9rem;color:#738078;margin-top:.8rem}
.book-copy .btn-row{margin-top:2rem}
.related{padding:56px 0;background:linear-gradient(145deg,#0b3027,#041914);color:white}
.related .ornament{text-align:center;font-size:1.15rem;margin-bottom:20px}
.related .ornament span{color:var(--green)}
.related .cover-row{max-width:900px;grid-template-columns:repeat(4,1fr)}

/* BAND */
.band{padding:50px 0;text-align:center;background:
 radial-gradient(circle at 50% 0,rgba(110,154,77,.18),transparent 55%),
 linear-gradient(#12382f,#071c17);color:white}
.band h2{font-size:clamp(2.2rem,5vw,4rem);line-height:.95;margin:.1rem 0 .7rem}
.band p{color:#d2ddd7;max-width:650px;margin:.4rem auto 1.5rem}

/* 404 */
.lost-hero{min-height:100vh;display:grid;align-items:center;position:relative;background:url("/images/hero-home.webp") center/cover no-repeat;color:white}
.lost-hero:after{content:"";position:absolute;inset:0;background:linear-gradient(90deg,rgba(2,16,12,.94),rgba(2,16,12,.65),rgba(2,16,12,.18))}
.lost-copy{position:relative;z-index:2;padding-top:80px;max-width:680px}
.lost-copy h1{font-size:clamp(4rem,10vw,7.5rem);line-height:.86;margin:.1rem 0 .8rem}
.lost-copy p{color:#e4eee8;font-size:1.08rem}

/* FOOTER */
.footer{background:#03150f;color:#b9cbc2;padding:35px clamp(22px,4vw,60px) 24px}
.footer-inner{width:min(1220px,100%);margin:auto;display:grid;grid-template-columns:1.2fr auto auto;gap:32px;align-items:center}
.footer-brand{display:grid;gap:4px}
.footer strong{color:white;letter-spacing:.08em}
.footer small{font-size:.78rem}
.footer a{text-decoration:none;color:white}
.footer-links{display:flex;gap:14px;font-size:.82rem;flex-wrap:wrap}
.footer-copy{font-size:.86rem}
.footer-meta{width:min(1220px,100%);margin:24px auto 0;padding-top:18px;border-top:1px solid rgba(255,255,255,.10);font-size:.76rem;color:#8fa49a}

/* RESPONSIVE */
@media(max-width:1080px){
 .main-nav{gap:.85rem}.main-nav a{font-size:.63rem}
 .home-intro{grid-template-columns:1.3fr repeat(3,.82fr)}.feature-card{grid-column:1/-1;min-height:170px}
 .cover-row{gap:18px}
}
@media(max-width:820px){
 .site-header{height:64px}.brand{min-width:130px}.brand strong{font-size:1.4rem}.menu-btn{display:block}
 .main-nav{display:none;position:absolute;top:62px;right:18px;flex-direction:column;align-items:stretch;background:rgba(4,29,22,.98);padding:14px 18px;border-radius:13px;box-shadow:0 18px 35px rgba(0,0,0,.25)}
 .main-nav.open{display:flex}
 .home-hero{height:700px;min-height:700px}.home-hero>img{object-position:58% 50%}.home-hero:after{background:linear-gradient(180deg,rgba(3,18,14,.48),rgba(3,18,14,.76) 55%,rgba(3,18,14,.88))}
 .hero-copy{padding-top:135px;text-align:center;margin:auto}.hero-copy .lede{margin-left:auto;margin-right:auto}.hero-copy .btn-row{justify-content:center}.hero-note{display:none}
 .home-intro{grid-template-columns:1fr 1fr}.home-intro>article{border-bottom:1px solid rgba(78,91,55,.18)}.home-intro>article:first-child,.feature-card{grid-column:1/-1}
 .cover-row{grid-template-columns:repeat(3,1fr)}
 .books-grid,.card-grid,.feature-grid{grid-template-columns:1fr 1fr}
 .split-feature,.about-layout,.book-detail{grid-template-columns:1fr}
 .split-art{order:-1}.split-art img{width:min(320px,75%)}
 .about-visual,.cover-stack{position:relative;top:auto}.about-visual{max-width:620px;margin:auto}.cover-stack{width:min(330px,82%);margin:auto}
 .related .cover-row{grid-template-columns:repeat(4,1fr)}
 .footer-inner{grid-template-columns:1fr}.footer-links{margin-top:4px}
}
@media(max-width:560px){
 .site-header{padding:0 16px}.brand span{font-size:.62rem}.brand strong{font-size:1.25rem}
 .home-hero{height:650px;min-height:650px}.home-hero>img{object-position:68% 50%}.hero-copy{padding-top:120px}.hero-copy h1{font-size:3.9rem}
 .home-intro{grid-template-columns:1fr;width:calc(100% - 24px)}.home-intro>article{border-right:0;padding:25px 21px}.feature-card{grid-column:auto}
 .cover-row{grid-template-columns:repeat(2,1fr);gap:16px}.book-showcase{padding-left:20px;padding-right:20px}
 .books-grid,.card-grid,.feature-grid{grid-template-columns:1fr}
 .sub-hero{min-height:315px;padding-top:118px}.sub-hero h1{font-size:3.3rem}
 .paper-section,.night-section{padding:58px 0}
 .book-hero{min-height:340px;padding-top:118px}.book-hero h1{font-size:3.2rem}
 .related .cover-row{grid-template-columns:repeat(2,1fr)}
}
@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important;transition:none!important}}
